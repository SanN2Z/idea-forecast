---
name: idea-forecast
description: "从「发展规律」到「最有价值的下一个 idea」的可复现选题流程，两条生成路线加一套过滤门。lineage：收引用最高的骨干论文 → 归纳已测量的规律 → 列脉络共享的前提、收「做到头也不行」的极限证据 → 否定前提推演新形态；shoots：收最近 90 天的新芽 → 聚成趋势 → 机制链外推预测 → 反推空位。两条路线的候选都要过同一套门（原子拆解 → 四轴检索 → 近邻差值 → 杀伤测试）。compare 模式同一方向两条都跑并对比哪条找到的 idea 更好。三段输出：发现的规律 / 已做的边界 / 未来可做的 idea。角色可配置（裁判 / 搜索者 A / 搜索者 B），可选接 ARIS 的 research-review 与 research-refine-pipeline。触发词：'找idea' '选题' '预测方向' '空位' '新芽' '这个有人做过吗' 'forecast'。"
argument-hint: "forecast <方向> [--mode lineage|shoots|compare] | check <idea>  [--project <idea-stage 目录>]"
allowed-tools: Bash(*), Read, Grep, Glob, Write, Edit, WebSearch, WebFetch, Agent, Skill
---

# idea-forecast：先规律、后预测、再过滤

## 这个 skill 解决什么

选题的常见死法有两种：一是先想出 idea 再去"确认"没人做过，检索词沿用自己的框架，结果被抢；二是把热点里的一个格子当方向，做完发现只是别人工作的增量。
本 skill 把顺序反过来并设硬门：**没有地形图不许预测；没有原子级近邻表不许说"空位"；没有杀伤测试不许开实验。**
生成 idea 的办法不是头脑风暴，而是：从引用最高的骨干论文里找到这条脉络所有工作共享的默认前提，收集"这条路做到头也解决不了"的证据，推演"没有这条前提的世界里下一步长什么样"；然后用最近 90 天的论文检查谁已经往外走了。爆点按定义不会被占，因为占位者都在现有前提之内。

产物固定为三段，对应组会最常问的三件事：**【发现的规律】【已做的边界】【未来可做的 idea】**。

## Context: $ARGUMENTS

## 角色与配置（先读，再开工）

四个角色，谁做什么写死；载体可换，写在 `<project>/idea-stage/terrain/CONFIG.md`（模板 `templates.md` §0）。

| 角色 | 默认载体 | 只做 | 不做 |
|---|---|---|---|
| **裁判 / 编排者** | 当前主会话（用手上最强的推理模型） | 拆原子；写检索 brief；差值分类；杀伤测试；列前提与极限证据；否定推演；押注与退路；**每个判断前亲自读卡片里的原句** | 不自己跑大规模检索；门 0–3 完成前不写任何预测 |
| **搜索者 A** | 子代理（`Agent`，显式指定模型，推荐 Opus 级；同一时刻只跑一个） | 读骨干与近期列表、读正文、写卡片（含英文原句、读取深度）、检索日志；把骨干聚成"已测量的事实" | 不外推、不判新颖、不写押注 |
| **搜索者 B** | 外部 CLI（Codex / Gemini / DeepSeek 任一；`scripts/search_b.py`）；没有就 `B=none` | 补 A 的盲区：相邻领域词汇、经典（≤2018）文献、产品与工程侧、对 A 的结果做独立交叉检索 | 不外推；结果只作线索，进卡片前须由 A 读正文或裁判读原句 |
| **本地索引与采集脚本** | `scripts/terrain_index.py`、`scripts/cited_backbone.py` | 门 0 查本项目已引用的论文；按引用数拉骨干、按日期拉近期 | — |

没有 B 时，A 必须把相邻领域与经典文献的查询也跑一遍，并在卡片里标 `[single-source]`。

## 产物目录

`<project>/idea-stage/terrain/`（跨 idea 复用，上一次的卡片下一次直接用）：
- `INDEX.md` / `index.json`：本项目所有文档里出现过的 arXiv 号与链接
- `backbone_<slug>_<date>.md`：引用骨干列表（脚本生成）；`recent_<slug>_<date>.md`：最近 N 天列表（脚本生成）
- `cards/<id>.md`：读过正文的论文卡（原子主张、测量对象、模型档位、证据原句、读取深度）
- `CLAIMS.md`：原子主张账本 + 检索日志
- `AXIOMS.md`：前提表 + 极限证据表 + 否定推演表
- `POSTMORTEM.md`：每次"被抢 / 早有人做"的复盘
- `briefs/`、`searches/`：给搜索者 B 的 brief 与它的输出
- `FORECAST_lineage_<date>.md` / `FORECAST_shoots_<date>.md`：各路线的三段输出；`COMPARE_<date>.md`：compare 模式的对比表

## 三种模式（两条生成路线 + 对比）

| 模式 | 生成器 | 近期论文的角色 | 用途 |
|---|---|---|---|
| `lineage` | 骨干（引用最高的论文）→ 归纳已测量的规律 → 列共享前提 → 收极限证据 → 否定前提推演新形态（F1a → F2 → F3 → F4） | 只在过滤阶段用：查谁已在前提之外、哪些候选已被做 | 先看清脉络再看新芽，避免被最新论文的框架带着走 |
| `shoots` | 近 90 天新芽 → 每颗芽写"解决了什么 / 默认前提 / 暴露了什么新问题 / 被广泛采用后会发生什么" → 聚成趋势（≥2 芽 + 数字）→ 机制链外推预测 → 反推空位（S1 → S2 → S3 → S4） | 就是原料 | 押最近的动向；骨干只用于门 0 与过滤 |
| `compare` | 两条都跑：同一方向、同一预算、卡片共享，推演各自独立落盘 → `COMPARE_<date>.md` | — | 回答"哪种方式找到的 idea 更好"；每次对比留档，攒够几次可看规律 |

compare 的对比维度（每个候选 idea 打分，裁判打，理由引卡片）：证据硬度（1–5；lineage 看极限证据，shoots 看芽的数量与数字）、门 3 差值类型（增量 / 整合型 / 可立项）、门 4 通过项数（0–4）、第一个实例的可做性（一个月内 / 一季度 / 更长）、被抢概率、两条路线是否得到同一个主押注。

## 路线一 lineage（F0 → F1a → F2 → F3 → F4 → F1b → F5 → F6）

### F0 地形先行（门 0，10 分钟）
1. `python <skill>/scripts/terrain_index.py --project <idea-stage> --build`，再 `--query <关键词>`。
2. `Grep` 本项目所有 .md 的关键词；`ls` 本机已 clone 的相关仓库并读 README 的方法节。
3. 输出一句：本地已有什么、哪些结论本项目已经写过。**本项目附录里已引用过的论文，后面不许当"新发现"。**

### F1a 引用骨干（脚本 + 搜索者 A）
1. `python <skill>/scripts/cited_backbone.py --project <idea-stage> --query "<方向词 1>" --query "<方向词 2>" --top 40 --since 2019`：按引用数拉前 40 篇，落 `backbone_*.md`。方向词至少两组（本领域词 + 一组相邻领域词），避免只拉到泛综述。
2. 搜索者 A 一次子代理：读骨干列表，剔除综述与不相关项，对余下每篇读正文写卡；标出每篇的**默认前提**与**测量到的数字**。综述只用来找它引用的原始工作，不作证据。

### F2 归纳（只写已发生的事）
把骨干卡按"它们共同指向的、已被测量的事实"聚类。每个事实至少 2 张卡、至少 1 个数字、至少 1 句英文原句。写成【归纳】段，不含任何外推。

### F3 极限推演（裁判亲做，这是生成器；产物 `AXIOMS.md`）
1. **列前提**：这条脉络所有工作共享的默认前提（frame axioms），每条一句，至少 4 条，来源是 F2 的事实卡。
2. **收极限证据**：每条前提配 ≥2 张全文卡，证据必须是"这条路做到头也解决不了"的那种（增益在公平评测下消失、判定器怎么做都不可靠、随规模坍缩），不是"还没做好"的那种。分不清的归后者。每条写"如果前提没错，会看到什么"。
3. **否定推演**：对每条前提做否定或替换，推演"没有它的世界里，下一步长什么样"：新对象、新目标量、什么不再需要、什么变成必需、机制链。同时问别的领域（生物、组织、控制、软件工程、经济学、心理测量）早就不用这条前提在活，它们的解法长什么样。
4. **排序**：按（极限证据硬度 × 现在无人往外走 × 我们能给出第一个可检验的实例）排序。高风险高回报是目标，不用"能不能发"过滤。

### F4 候选新形态与第一个实例
对前 2–3 条各写：新形态一句话；第一个可检验实例（小到一个月能做，写清臂、主量、判据）；成立时推翻什么、不成立时说明什么。**此时不看近期论文**：先把自己的推演落盘，再看别人。

### F1b 最近 90 天的新芽，用于过滤（脚本 + 搜索者 A + 搜索者 B）
1. `python <skill>/scripts/cited_backbone.py --project <idea-stage> --query "..." --recent-days 90`：按提交日期拉 arXiv 近 90 天，落 `recent_*.md`。
2. 搜索者 A：对 F4 的每个候选，在近期列表里找"已经在这条前提之外工作"的论文，读正文写卡；同时补五类来源的芽（核心回路 / 评测与接受 / 相邻系统 / 模型侧挤压 / 产品与基础设施）。**只读摘要的标 `[abstract-only]`，不能进机制链。**
3. 搜索者 B 同时跑相邻领域 + 经典 + 产品侧的 brief。

## 路线二 shoots（F0 → S1 → S2 → S3 → S4 → F5 → F6）

### S1 收芽（脚本 + 搜索者 A + 搜索者 B）
`cited_backbone.py --recent-days 90` 拉近 90 天列表；A 按五类来源（核心回路 / 评测与接受 / 相邻系统 / 模型侧挤压 / 产品与基础设施）各挑 3–8 颗，每颗读正文写卡：出处日期、解决了什么、默认前提、暴露了什么新问题、"被广泛采用后会发生什么"。B 补产品侧（changelog、博客、issue）与相邻领域的芽。**只读摘要的标 `[abstract-only]`，不能进机制链。**

### S2 归纳趋势（只写已发生的事）
把芽按"它们共同指向的、已被测量的事实"聚类：每个趋势至少 2 颗芽、至少 1 个数字、至少 1 句原句。写成【归纳】段，不含外推。

### S3 预测（裁判亲做）
对每个趋势写机制链外推：如果这个趋势持续 6–12 个月，什么会先撞墙、什么会变成瓶颈、什么会被淘汰。每条预测写：机制链、时间窗、可观察标志、"错了会看到什么"。预测必须引用 ≥3 颗芽的卡片 ID。

### S4 反推空位与候选
从每条预测反推"到那时谁都需要、现在没人做"的对象，写成候选新形态 + 第一个可检验实例（同 F4 的格式）。骨干（F1a，可以只跑脚本不写卡）在这里只用来核对候选是不是老问题的重提。

## 过滤与输出（两条路线共用）

### F5 过滤（门 1–4，每个候选都要过）
- **门 1 拆原子**：E 经验前提 / M 机制 / A 构件；每条写测量单元、目标量、依赖；组合型主张单独成行写成"原子 1 + 原子 2 的接合处"。
- **门 2 地形图**：每个原子按 `templates.md` 的检索矩阵：四种词汇 × 三个时间片 × 三类来源；每原子 ≥8 条查询，最近 3 篇读正文。
- **门 3 近邻差值表**：最近 3 篇 | 测量单元 | 目标量 | 与本原子的精确差值。差值三类：单元替换/加列/改名 = 增量；两个已有原子的接合 = 整合型（给 12 个月被抢概率与退路）；新对象/新测量/别人没有仪器 = 可立项。对"前提之外"的候选放宽：近邻仍在原前提之内的不算先例。填竞争者画像。
- **门 4 杀伤测试**（缺一不可）：前提已测？构件即构念？骨架已有？可检出？最便宜的证伪路径？四项全过才写预注册，否则回门 1 改原子。

### F6 输出（三段物理分开，`FORECAST_<mode>_<date>.md`）
1. **【发现的规律】**：F2 的事实（数字与原句）+ F3 的前提表与极限证据表。
2. **【已做的边界】**：门 3 近邻差值表（≤10 行）+ 竞争者画像 + 近 90 天里已在前提之外的工作 + 本地已有的仪器与数据。
3. **【未来可做的 idea】**：主押注（新形态、第一个实例、判据、退路）、依赖的预测（机制链、时间窗、可观察标志、"错了会看到什么"）、至少一个不同方向的备选、被抢概率。
"没人做过"只能写成"在以上检索范围内未见"，近邻表原样附上。

### F7 可选：接 ARIS
- 门 4 之后要跨模型评审：`/research-review <FORECAST 文件>`。
- 预注册之后要直接出实验计划：`/research-refine-pipeline <FORECAST 文件>`。
- 只做查新：`/novelty-check` 可作门 2 的补充检索器，结论回填成本 skill 的近邻矩阵格式。

## 模式 check：已有 idea 的过滤

`/idea-forecast check <idea>`：跳过生成路线，直接把 idea 过门 0–4，输出一句判定（增量 / 整合型 / 可立项）、原子表、近邻表、杀伤测试结果、"若不可立项，改哪个原子"。

## 硬规则

- 门 0–3 完成前禁止出现"预测""押注""爆点"字样；lineage 路线 F4 落盘前不许看近期列表，shoots 路线 S3 落盘前不许看骨干的推演。
- 卡片必须有英文原句与读取深度；`[abstract-only]` 不能支撑"未见先例"，不能进机制链。综述不作证据。
- 只被一方（A 或 B）命中的近邻标 `[single-source]`，补读正文后才能进近邻矩阵；两方都没命中才能写"在检索范围内未见"。
- 裁判读的是原句，不是搜索者的总结句。
- 地形图预算：A 子代理一次一个、串行；不够就缩小原子集，不加并发。
- 每次被抢，在 `POSTMORTEM.md` 记：哪个门本可拦住、缺的检索词、是否本地已有；并把缺的词加进 `templates.md` 的相邻领域词表。这是本 skill 自我改进的唯一入口。

## 输出给用户的格式

1. 一句判定（forecast：主押注一句话 + 风险等级；compare：两条路线的主押注是否一致、哪条更硬；check：增量 / 整合型 / 可立项）。
2. 三段：发现的规律 / 已做的边界 / 未来可做的 idea（各不超过一页）。
3. 附：原子表、近邻表、杀伤测试四项、竞争者画像、卡片清单（含读取深度）。
